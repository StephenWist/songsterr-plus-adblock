// if we get the 'showroom' section songsterr seems to be able to detect this and recreate it. So we get its child div. Murder the child.
document.getElementById('showroom').firstChild.remove();

document.getElementById('promo').remove();
